{
    "name": "ecommerce-monorepo",
    "version": "1.0.0",
    "description": "Monorepo for Spring Boot Backend and Angular Frontend",
    "scripts": {
        "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm run start:backend\" \"npm run start:frontend\"",
        "start": "npm run dev",
        "start:backend": "cd backend && ./mvnw spring-boot:run",
        "start:frontend": "cd frontend && npm start",
        "install:all": "npm install && cd frontend && npm install && cd ../backend && ./mvnw install -DskipTests",
        "build": "npm run build:frontend && npm run build:backend",
        "build:frontend": "cd frontend && npm run build",
        "build:backend": "cd backend && ./mvnw package -DskipTests"
    },
    "dependencies": {
        "concurrently": "^9.1.2"
    }
}